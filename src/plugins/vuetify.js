import Vue from 'vue';
import Vuetify from 'vuetify/lib/framework';

Vue.use(Vuetify);

//Librerias de Vue, para que no sea HTML puro, que brinda componentes html prefabricados para Vue

export default new Vuetify({
});
